import styled from '@emotion/styled';
import MuiDialogActions from '@mui/material/DialogActions';

const PopUpActions = styled(MuiDialogActions)`
  margin: 0;
  padding: 14px 0px;
`;

export default PopUpActions;
